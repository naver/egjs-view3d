---
sidebar_position: 2
---

import View3D, { WhiteLightPreset } from "@site/src/components/View3D";
import OptionDescriptor from "@site/src/components/OptionDescriptor";

<OptionDescriptor type="string" defaultVal={`"auto"`} />

File extension format of the 3D model (glTF, glb, ...)
If `"auto"` is given, View3D will try to detect the format of the 3D model with the name of `src` URL

Available formats:
- See [MODEL_FORMAT](/docs/api/MODEL_FORMAT)

```ts
new View3D("#el", {
  src: "SOURCE_TO_YOUR_3D_MODEL",
  format: "glTF"
})
```

## Example
<View3D src="/model/bunny.glb" format="glb" preset={new WhiteLightPreset()} />

```ts
import View3D, { WhiteLightPreset } from "@egjs/view3d";

new View3D("#view3d-canvas", {
  src: "/model/bunny.glb",
  format: "glb",
  preset: new WhiteLightPreset()
})
```
